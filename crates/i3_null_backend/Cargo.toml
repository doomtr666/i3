[package]
name = "i3_null_backend"
version.workspace = true
edition.workspace = true
authors.workspace = true

[dependencies]
i3_gfx.workspace = true
log.workspace = true # I'll need to add log to workspace deps as it's justified for an oracle
