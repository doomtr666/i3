[package]
name = "vulkan_diagnostics"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
